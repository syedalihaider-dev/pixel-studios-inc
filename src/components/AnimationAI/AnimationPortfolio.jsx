"use client";
import React from "react";
import GlobalAnimationPortfolio from "../Common/AnimationPortfolio";

export default function AnimationPortfolio() {
  return (
    <GlobalAnimationPortfolio
      subtitle="Our Work Speaks"
      heading="Our AI Animation Portfolio"
      description="AI animation is new enough that most of the strongest work in the category does not announce itself as AI-produced. We produce content that works. The production method is the part that the audience does not need to know."
      variant="gradient"
    />
  );
}
