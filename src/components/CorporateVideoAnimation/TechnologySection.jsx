"use client";
import React, { useState } from 'react';
import styles from './CooperationModelsSection.module.css';
import { motion } from 'framer-motion';
import Image from 'next/image';
import { Swiper, SwiperSlide } from 'swiper/react';
import { Navigation, Autoplay } from 'swiper/modules';
import { ArrowLeft, ArrowRight } from 'lucide-react';

import 'swiper/css';
import 'swiper/css/navigation';

const CooperationModelsSection = () => {
  const [swiperRef, setSwiperRef] = useState(null);

  const cards = [
    {
      id: 1,
      icon: "/game-development/outstaffing.png",
      title: "Animation Style and Complexity",
      text: "Motion graphics corporate videos cost less than fully illustrated character-driven productions with custom assets built from scratch."
    },
    {
      id: 2,
      icon: "/game-development/dedicated-team.png",
      title: "Video Duration",
      text: "A 60-second explainer costs less than a 3-minute company overview with multiple scenes and distinct animation segments."
    },
    {
      id: 3,
      icon: "/game-development/managed-outsourcing.png",
      title: "Script and Creative Requirements",
      text: "Projects requiring discovery-led scriptwriting and multiple creative concept directions add to the production scope."
    },
    {
      id: 4,
      icon: "/game-development/managed-outsourcing.png",
      title: "Voiceover and Sound Design",
      text: "Professional voiceover, music licensing, and custom sound design are included in standard packages and priced transparently."
    },
    {
      id: 5,
      icon: "/game-development/managed-outsourcing.png",
      title: "Production Timeline",
      text: "Rush corporate animation video production is available and priced honestly without absorbing the premium and delivering lower quality."
    }
  ];

  return (
    <section className={styles.cooperationModelsSection}>
      <motion.div
        className="container"
        initial={{ opacity: 0, y: 50 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true, margin: "-100px" }}
        transition={{ duration: 0.8 }}
      >
        <div className="row justify-content-center text-center mb-4">
          <div className="col-12 col-lg-12">
            <p className="subtitle">Pricing Factors</p>
            <h2 className={styles.mainHeading}>How Much Does Corporate Video Animation Cost?</h2>
            <p className={styles.topParagraph}>
              Corporate video animation cost is driven by five variables, and we price every project transparently based on your actual scope.
            </p>
          </div>
        </div>

        {/* Swiper Slider */}
        <Swiper
          onSwiper={setSwiperRef}
          modules={[Navigation, Autoplay]}
          spaceBetween={30}
          slidesPerView={3}
          loop={true}
          autoplay={{ delay: 5000, disableOnInteraction: false }}
          breakpoints={{
            0: { slidesPerView: 1, spaceBetween: 15 },
            768: { slidesPerView: 2, spaceBetween: 20 },
            1024: { slidesPerView: 3, spaceBetween: 30 }
          }}
          className={styles.swiperContainer}
        >
          {cards.map((card) => (
            <SwiperSlide key={card.id} className={styles.swiperSlide}>
              <div className={styles.modelCard}>
                <div className={styles.iconWrapper}>
                  <Image
                    src={card.icon}
                    alt={card.title}
                    width={61}
                    height={61}
                  />
                </div>
                <h3 className={styles.cardTitle}>{card.title}</h3>
                <p className={`${styles.cardParagraph} scroll_block`}>
                  {card.text}
                </p>
              </div>
            </SwiperSlide>
          ))}
        </Swiper>

        {/* Bottom Circle Navigation Buttons */}
        <div className={styles.navigationWrapper}>
          <button
            type="button"
            className={styles.navBtn}
            onClick={() => swiperRef?.slidePrev()}
            aria-label="Previous Slide"
          >
            <ArrowLeft size={22} className={styles.navIcon} />
          </button>
          <button
            type="button"
            className={styles.navBtn}
            onClick={() => swiperRef?.slideNext()}
            aria-label="Next Slide"
          >
            <ArrowRight size={22} className={styles.navIcon} />
          </button>
        </div>
      </motion.div>
    </section>
  );
};

export default CooperationModelsSection;
